extends layout

block content
  h1= title 
  - let items = results
  div(class="container-fluid")
    div(class="row")
        each item in items
            div(class="col-sm-2") #{item.name} #{item.color} #{item.hardness}
